# ChainPulse Codebase Integration Plan

Following up on the generated designs, the next step is to actually scaffold the Next.js frontend application and port the HTML/CSS generated by Stitch MCP into the working codebase.

## User Review Required

> [!IMPORTANT]
> Please review this plan. I will initialize a new Next.js project in the `d:\StitchProjects\chainpulse` directory. This will set up the boilerplate React structure where the Stitch designs will live. Since this will format your project folder, I need your approval.

## Proposed Changes

### Component: Project Scaffold
- Install Next.js (Pages Router) using vanilla CSS.
- **Command**: `npx -y create-next-app@latest . --ts --eslint --no-tailwind --no-app --no-src-dir --import-alias "@/*"`

### Component: Design Integration (Landing Page)
#### [MODIFY] `styles/globals.css`
- I will inject the proprietary "Aether Obsidian" CSS variables and token stylesheets generated by the Stitch Design agent.

#### [MODIFY] `pages/index.tsx`
- Complete translation of the Landing Page HTML into a React functional component.
- The layout will include the Hero section with the AI Mascot and the Dune + AI features grid.

### Component: Design Integration (Dashboard)
#### [NEW] `pages/dashboard.tsx`
- Translate the Dashboard HTML generated by Stitch into a React component.
- The dashboard will contain the Sidebar, Top KPIs, Main Chart area, and the AI Insights Feed.

## Open Questions

> [!WARNING]
> Since we currently only have static HTML files from the Stitch outputs:
> - Will you be providing real React component code for complex charts (like `recharts` or `chart.js`), or should I convert the static SVG/HTML charts generated by the design agent as placeholders for now?
> - Is there any specific existing code in the folder that shouldn't be overwritten, or is it safe to initialize the Create Next App right in the root?

## Verification Plan

### Automated Tests
- Scaffold the project.
- Run `npm run build` to ensure the React translation has perfectly typed TSX and compiles without errors.
- Run `npm run dev` in the background and test that the pages load properly locally.
